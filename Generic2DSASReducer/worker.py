#!/usr/bin/python3.8
"""This is the worker module that handles the top user interface."""

import numpy as np

from loader.saxsans import SaxansLoader
from twodoperations.twodoperations import DataSet

class Worker:
  
  def _setup_worker(self, parameter_file):
  
  def __init__(self, parameter_file):
    self._setup_worker(parameter_file)